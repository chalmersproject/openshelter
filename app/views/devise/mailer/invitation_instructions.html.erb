<p>
  <%= t(
    "devise.mailer.invitation_instructions.hello",
    email: @resource.email,
  ) %>,
</p>
<p>
  You've been invited to <%= link_to "Chalmers", :root %>! You can finish
  creating your account through the below:
</p>
<p>
  <%= link_to(
    "Complete registration",
    accept_invitation_url(@resource, invitation_token: @token),
  ) %>
</p>
<% if @resource.invitation_due_at %>
  <p>
    <%= t(
      "devise.mailer.invitation_instructions.accept_until",
      due_date: l(
        @resource.invitation_due_at,
        format: "devise.mailer.invitation_instructions.accept_until_format",
      )
    ) %>
  </p>
<% end %>
<p>
  <%= t("devise.mailer.invitation_instructions.ignore") %>
</p>
