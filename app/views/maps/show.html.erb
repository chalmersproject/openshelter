<%= turbo_stream_from "shelter_measurements" %>
<div class="flex-1 flex flex-col relative">
  <%= render ShelterLocationsComponent.new(
    shelters: @shelters,
    interactive: true,
    userAgent:device,
    class: "flex-1 !rounded-none",
  ) %>
</div>
